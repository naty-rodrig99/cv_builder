import Link from "next/link";
import { getUser } from "~/server/user";
import LogoutButton from "~/app/auth/logout/logout-button";
import { Button } from "~/components/ui/button";
import { WebsiteName, H0, P1 } from "~/components/ui/typography";
import {
  routeLogin,
  routeProfile,
  routeProjects,
  routeSignup,
} from "~/app/routes";

const navigation = [
  { name: "Projects", href: routeProjects() },
  { name: "Profile", href: routeProfile() },
];

export default async function HomePage() {
  const user = await getUser();

  return (
    <div className="bg-white">
      <header className="absolute inset-x-0 top-0 z-50">
        <nav
          aria-label="Global"
          className="flex items-center justify-between p-6 lg:px-8"
        >
          <div className="flex lg:flex-1">
            <WebsiteName>
              <Link href="/">CV Builder</Link>
            </WebsiteName>
          </div>
          <div className="hidden lg:flex lg:gap-x-12">
            {navigation.map((item) => (
              <a
                key={item.name}
                href={item.href}
                className="text-sm font-semibold leading-6 text-gray-900"
              >
                {item.name}
              </a>
            ))}
          </div>
          <div className="hidden lg:flex lg:flex-1 lg:justify-end">
            {!user && (
              <Button asChild variant="default">
                <Link href={routeSignup()}>Sign Up</Link>
              </Button>
            )}
            {!user && (
              <Button asChild variant="loginBtn">
                <Link href={routeLogin()}>Log in &rarr;</Link>
              </Button>
            )}
            {user && <LogoutButton />}
          </div>
        </nav>
      </header>
      <div className="relative isolate px-6 pt-14 lg:px-8">
        <div
          aria-hidden="true"
          className="absolute inset-x-0 -top-40 -z-10 transform-gpu overflow-hidden blur-3xl sm:-top-80"
        >
          <div
            style={{
              clipPath:
                "polygon(74.1% 44.1%, 100% 61.6%, 97.5% 26.9%, 85.5% 0.1%, 80.7% 2%, 72.5% 32.5%, 60.2% 62.4%, 52.4% 68.1%, 47.5% 58.3%, 45.2% 34.5%, 27.5% 76.7%, 0.1% 64.9%, 17.9% 100%, 27.6% 76.8%, 76.1% 97.7%, 74.1% 44.1%)",
            }}
            className="relative left-[calc(50%-11rem)] aspect-[1155/678] w-[36.125rem] -translate-x-1/2 rotate-[30deg] bg-gradient-to-tr from-[#ff80b5] to-[#9089fc] opacity-30 sm:left-[calc(50%-30rem)] sm:w-[72.1875rem]"
          />
        </div>
        <div className="mx-auto max-w-2xl py-32 sm:py-48 lg:py-56">
          <div className="text-center">
            <H0>Craft Your Future - Build a Standout CV in Minutes!</H0>
            <P1>
              Choose a template or design your own CV. Collaborate with others
              and showcase your skills. Build a resume that truly represents
              you!
            </P1>
            <div className="mt-10 flex items-center justify-center gap-x-6">
              <Button asChild variant="colorBtn">
                <Link href="/projects">Get Started</Link>
              </Button>
            </div>
          </div>
        </div>
        <div
          aria-hidden="true"
          className="absolute inset-x-0 top-[calc(100%-13rem)] -z-10 transform-gpu overflow-hidden blur-3xl sm:top-[calc(100%-30rem)]"
        >
          <div
            style={{
              clipPath:
                "polygon(74.1% 44.1%, 100% 61.6%, 97.5% 26.9%, 85.5% 0.1%, 80.7% 2%, 72.5% 32.5%, 60.2% 62.4%, 52.4% 68.1%, 47.5% 58.3%, 45.2% 34.5%, 27.5% 76.7%, 0.1% 64.9%, 17.9% 100%, 27.6% 76.8%, 76.1% 97.7%, 74.1% 44.1%)",
            }}
            className="relative left-[calc(50%+3rem)] aspect-[1155/678] w-[36.125rem] -translate-x-1/2 bg-gradient-to-tr from-[#ff80b5] to-[#9089fc] opacity-30 sm:left-[calc(50%+36rem)] sm:w-[72.1875rem]"
          />
        </div>
      </div>
    </div>
  );
}
